# Вхідні дані
t1, t2, t3 = map(int, input().split())

# Розрахунок сумарної швидкості поїдання пирога
total_speed = (1 / t1) + (1 / t2) + (1 / t3)

# Розрахунок часу, потрібного для з'їдання пирога трьома гостями разом
total_time = 1 / total_speed

# Виведення результату з округленням до 2 знаків після коми
print(f"{total_time:.2f}")

